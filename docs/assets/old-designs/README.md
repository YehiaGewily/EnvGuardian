# Archived logo concepts

These are the runner-up marks from the original logo exploration. The project uses the
**keyhole** mark ([`../logo-mark.svg`](../logo-mark.svg)); these are kept for reference only
and are **not** used anywhere in the project.

- `logo-mark-terminal.svg` — shield + terminal `>_` (sky→indigo).
- `logo-mark-key.svg` — shield + key (violet).
