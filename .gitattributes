# Golden files are compared byte-for-byte against LF program output; never let
# git rewrite their line endings on checkout (Windows would break the tests).
*.golden text eol=lf

# Go sources are LF everywhere.
*.go text eol=lf
